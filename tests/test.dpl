fn main() -> i32 {
    enum Foo {
        Bar,
        Baz,
    }

    let foo: Foo = Foo::Baz;

    let msg: str = match foo {
        Foo::Bar => {
            "hello"
        }
        Foo::Baz => {
            "world"
        }
    };
    printf(msg);

    let x: str = "foo";
    printf(x);

    0
}
