fn main() -> i32 {
    enum Foo {
        Bar,
        Baz,
    }

    let foo: Foo = Foo::Baz;

    match foo {
        Foo::Bar => {
            1
        }
        Foo::Baz => {
            0
        }
    }
}
